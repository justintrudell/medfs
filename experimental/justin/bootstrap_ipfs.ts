/*
function addBootstrapPeer(): void {
  ipfsNode.bootstrap.add(remoteNode, (err: any, _res: any) => {
    if (err) {
      console.error(`Failed to connect to bootstrap host: ${err}`);
      return;
    }
    console.log("Bootstrap success!");
    console.log(_res);
  });
}
*/
