{
  "name": "medfs_client",
  "version": "0.0.2",
  "description": "medfs_client in electron",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "start": "npm run build && electron ./out/ui/main.js",
    "console": "npm run build && node"
  },
  "dependencies": {
    "@types/jest": "^20.0.2",
    "@types/node": "^10.3.2",
    "@types/react": "^15.0.33",
    "@types/react-dom": "^15.5.1",
    "request": "2.87.0"
  },
  "devDependencies": {
    "electron": "^2.0.2",
    "tslint": "^5.10.0",
    "typescript": "^2.8.3"
  }
}
